import { SecretKeyDirective } from './secret-key.directive';

describe('SecretKeyDirective', () => {
  it('should create an instance', () => {
    const directive = new SecretKeyDirective();
    expect(directive).toBeTruthy();
  });
});
