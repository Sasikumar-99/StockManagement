import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'firstProjectIonic',
  webDir: 'www',
  bundledWebRuntime: true
};

export default config;
